---
const { class: className, id, title } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  {title && (<h2 class="mb-6 text-3xl text-center sm:text-left font-semibold text-black/80 dark:text-white">{title}</h2>)}
  <slot />
</section>